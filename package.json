{
  "name": "aviary",
  "version": "0.0.1",
  "description": "Quản lý nhiều tài khoản X qua profile chromium anti-detect, hẹn giờ đăng bài, tích hợp n8n",
  "private": true,
  "author": "",
  "license": "UNLICENSED",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "echo \"chưa cấu hình - sẽ thêm ở MVP\" && exit 0",
    "build": "echo \"chưa cấu hình - sẽ thêm ở MVP\" && exit 0",
    "lint": "echo \"chưa cấu hình\" && exit 0",
    "test": "echo \"chưa có test\" && exit 0"
  },
  "engines": {
    "node": ">=20"
  }
}
